# Path al file JSON delle credenziali del Service Account
GOOGLE_SERVICE_ACCOUNT_FILE=./credentials.json

# Email del super admin Google Workspace (obbligatorio)
GOOGLE_ADMIN_EMAIL=admin@tuodominio.it

# Customer ID del dominio (default: my_customer = dominio corrente)
GOOGLE_CUSTOMER_ID=my_customer
